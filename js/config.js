/*
 * Baja Cape Loop — site configuration
 * ------------------------------------
 * The map needs a PUBLIC Mapbox GL token (starts with "pk."). It is NOT a
 * secret — public tokens are safe to ship in a static site — but you still
 * have to supply your own. Get one free at https://account.mapbox.com/.
 *
 * Replace the placeholder below with your token, e.g.:
 *   window.MAPBOX_TOKEN = "pk.eyJ1Ijoi...your-token...";
 *
 * If you leave the placeholder as-is, the site still works completely —
 * the Map tab shows a friendly fallback and the Itinerary, Gear, and
 * Readiness tabs are fully functional without any token.
 */
window.MAPBOX_TOKEN = "pk.PASTE_YOUR_MAPBOX_PUBLIC_TOKEN_HERE";
